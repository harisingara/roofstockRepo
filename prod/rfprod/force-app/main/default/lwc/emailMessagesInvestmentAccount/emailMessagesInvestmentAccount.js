import { LightningElement,api,track } from 'lwc';
import fromAddress from '@salesforce/apex/EmailMessages.fromAddress';
export default class EmailMessagesInvestmentAccount extends LightningElement {
    @api recordId;

    
}